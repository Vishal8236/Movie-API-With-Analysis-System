<div class="container-fluid">
    <div class="row mt-3">
        <div class="col-sm">
            <div class="top-sec pt-3">
                <span class="h4 text-muted font-weight-bold">Hello <%= get_first_name(current_user.name) %>, <%=gretting_user%></span>
            </div>
            <div class="show-api mt-5">
                <% if has_api_key? %>
                    <div class="col-9 border rounded p-2 shadow-3">
                        <span class="">📌 For developers</span>
                        <div class="mt-3">
                            <span id="sayHello">Key - <strong id="key"><%= current_user.user_api_key.apikey %></strong></span>
                            <button class="btn btn-sm btn-primary" onclick="copyToClipboard(key)" id="copykey">Copy Key</button>
                        </div>
                    </div>
                <% else %>
                    <div class="col-9 border rounded p-2 shadow-3">
                        <button class='btn btn-primary btn-sm'>Generate Your API Key</button>
                    </div>
                <% end %>
            </div>
        </div>
        <div class="col-sm">
        </div>
    </div>
</div>